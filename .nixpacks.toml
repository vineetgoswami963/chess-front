[phases.setup]
engine = "nodejs-14_x"

[dependencies]
python = "python3_10"   # Specify Python 3.10 (or python3_9 if 3.10 isn’t available)

[phases.build]
run = "react-scripts build"  # Or `yarn install` if using Yarn

[phases.start]
run = "react-scripts start"  # Replace with your start command
